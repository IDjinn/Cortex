# Backend API base URL. Use your machine's LAN IP for physical device testing.
EXPO_PUBLIC_API_BASE_URL=http://localhost:5172
# OAuth redirect URI (must match backend + app scheme).
EXPO_PUBLIC_OAUTH_REDIRECT=cortex://auth/callback
