import { ComingSoon } from '@/components/screens/ComingSoon';

/** Skills manager (Phase 3): SKILL.md format, per-conversation enablement. */
export default function SkillsScreen() {
  return (
    <ComingSoon
      title="Skills"
      glyph="✦"
      description="Skills reutilizáveis no formato aberto SKILL.md — criar, editar e habilitar por conversa, com diretório curado."
      phase="Fase 3 do roadmap"
    />
  );
}
