import { ComingSoon } from '@/components/screens/ComingSoon';

/** Tasks inbox (Phase 4): standalone chats and delegated sub-tasks. */
export default function TasksScreen() {
  return (
    <ComingSoon
      title="Tarefas"
      glyph="☑"
      description="Chats avulsos e sub-tarefas delegadas ao agente principal, separados das conversas dos projetos."
      phase="Fase 4 do roadmap"
    />
  );
}
