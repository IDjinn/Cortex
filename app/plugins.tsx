import { ComingSoon } from '@/components/screens/ComingSoon';

/** Plugin system (Phase 4): on top of skills + MCP + tools. */
export default function PluginsScreen() {
  return (
    <ComingSoon
      title="Plugins"
      glyph="⬢"
      description="Extensões sobre skills, MCP e ferramentas — instalar, habilitar e configurar por projeto."
      phase="Fase 4 do roadmap"
    />
  );
}
