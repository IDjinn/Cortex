import { ComingSoon } from '@/components/screens/ComingSoon';

/** Slash commands (Phase 4): reusable prompt templates, global or per project. */
export default function CommandsScreen() {
  return (
    <ComingSoon
      title="Commands"
      glyph="/"
      description="Comandos de barra com templates de prompt reutilizáveis — invoque com “/” no composer, no escopo global ou por projeto."
      phase="Fase 4 do roadmap"
    />
  );
}
